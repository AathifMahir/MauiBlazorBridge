@namespace MauiBlazorBridge
@inject IBridge Bridge

@if(Bridge.Framework == FrameworkIdentity.Maui)
{
    @Maui
}
else
{
    @Blazor
}

@code {

    [Parameter]
    public RenderFragment Maui { get; set; } = default!;

    [Parameter]
    public RenderFragment Blazor { get; set; } = default!;
}
